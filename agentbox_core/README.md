# Agentbox Skills Core

This package contains the Python runtime for the tools-only `agentbox_skills` project.

It exposes:

- on-chain read helpers
- on-chain write helpers
- prerequisite checks
- world and role snapshot builders for dialogue planning

It intentionally excludes:

- autonomous goal generation
- autonomous operation generation
- reflection
- background workers
